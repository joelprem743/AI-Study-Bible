import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joelpremtej.biblecompanion',
  appName: 'Bible Companion App',
  webDir: 'dist'
};

export default config;
